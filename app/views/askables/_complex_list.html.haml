- if item.keys[0] > 0
  .answerable.yes
    .answerer
      - item.values[0].each do |v|
        = user_small_avatar_link(v.user)
      / voted affirmatively
- else
  .answerable.no
    .answerer
      / nay vote#{ 's' if item.values[0].size > 1 } by
      - item.values[0].each do |v|
        = user_small_avatar_link(v.user)