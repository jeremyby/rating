
div = $("<%= j(render 'askables/follow') %>").hide()

$('#sph .blocks').children().first().replaceWith(div)

if (div.hasClass('table'))
  div.show('highlight')
else
  div.show()
  div.children('.table').effect('highlight')

