- poll = Poll.where('id = ?', entry.poll_id).includes(:owner, :country).first

.majority
  .question
    Question
    - if show_country
      of
      %span= link_to poll.country, country_path(poll.country)
    has result!
    
    = link_to poll.question_answers, country_poll_path(poll.country, poll)
  .info
    The answer of the people is
    %em= poll.yes_count > poll.no_count ? poll.yes : poll.no
  