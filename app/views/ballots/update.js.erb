$("#spb .lc .ata").html("<%= j render('polls/edit_ballot') %>");

if ($("#ballots h2").next().length) {
  if ($('#ballot_<%= @ballot.id %>').length)
    $('#ballot_<%= @ballot.id %>').remove();

  $("#ballots h2").after("<%= (j (render 'polls/complex_item', :ballot => @ballot)) %>");
}