$('#ltpcs').text("<%= pluralize(@commentable.comment_threads.size, 'comment') %>")
$('#poll_comments').replaceWith("<%= escape_javascript(render 'polls/poll_comments')%>");
$('#poll_comments .detail').show()